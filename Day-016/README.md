# День [16]: [Project 1, part one]

## 📅 Дата: _[25.03.2026]_

## 📖 Що вивчив
<!-- Опиши своїми словами що ти дізнався сьогодні -->
-   здійснив історичний перехід від Playgrounds до повноцінних iOS-додатків! створив свій перший проєкт WeSplit. познайомився з інтерфейсом Xcode, Canvas та Preview. вивчив базові цеглинки SwiftUI: VStack (вертикальний стек), Form (форма налаштувань) та NavigationStack (панель навігації). розібрався з магією стану за допомогою обгортки @State та двостороннім зв'язуванням $. на завершення навчився генерувати інтерфейс автоматично за допомогою циклу ForEach та елемента Picker.

## 💡 Ключові інсайти
<!-- Що тебе здивувало або що здалося особливо важливим? -->
-   SwiftUI — це декларативний фреймворк, який працює з легкими структурами (struct). оскільки структури не можна змінювати, Apple створила суперсилу @State, яка ховає дані у надійний "сейф" у пам'яті, дозволяючи інтерфейсу оновлюватися. двостороннє зв'язування $ — це портал, який одночасно читає дані і записує нові (наприклад, з TextField). а ще розкрив секрет $0 — це просто зручне скорочення для першого аргументу в замиканнях (closures).

## 🤔 Що було складно
<!-- Чесно запиши що було незрозуміло — це допоможе відстежити прогрес -->
-   перелаштувати мислення з імперативного підходу (крок за кроком) на декларативний (просто описуємо, що хочемо бачити). було важливо зрозуміти різницю між $name (двосторонній зв'язок) та $0 (тимчасовий параметр у циклі). також розбирався з тим, чому ForEach вимагає id: \.self для ідентифікації кожного рядка тексту.

## 💻 Код дня
<!-- Вставити найцікавіший фрагмент коду або посилання на файл -->
```swift
// Твій код тут
```

## 📝 Нотатки з обговорення з AI
<!-- Короткий підсумок обговорення -->
-   детально тестували модифікатори (як-от .padding() і .background()), щоб фізично побачити межі елементів. розібрали, чому ForEach(0...100) видає помилку (бо функція чекає напіввідкритий діапазон ..<). затвердили правило не поспішати: розбирати кожен рядок до гвинтиків важливіше, ніж просто копіювати код із туторіалу.

## 🖼️ Скріншоти (якщо є)
<!-- Додай скріншоти UI якщо працював з SwiftUI -->
<!-- ![Опис](screenshot.png) -->

## ✅ Прогрес: [16]/101 (WeSplit Part 1: фундамент закладено!)
