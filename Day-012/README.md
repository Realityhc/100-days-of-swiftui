# День [X]: [Day 12 – Classes, inheritance, and checkpoint 7]

## 📅 Дата: _[16.03.26]_

## 📖 Що вивчив
<!-- Опиши своїми словами що ти дізнався сьогодні -->
-   ознайомився з Класами (Classes) — ще одним способом створення власних типів даних. дізнався їхні 5 головних відмінностей від структур: успадкування (inheritance), відсутність автоматичного ініціалізатора, копіювання посилань (а не значень), наявність деініціалізаторів (deinit) та можливість змінювати змінні властивості навіть у константних екземплярах (let).

## 💡 Ключові інсайти
<!-- Що тебе здивувало або що здалося особливо важливим? -->
-   найважливіша фішка класів — це те, що всі їхні копії вказують на одні й ті самі дані в пам'яті (як спільний документ у Google Docs). якщо змінити одну копію, зміняться всі! також розібрався, що дочірній клас може змінювати поведінку батьківського за допомогою ключового слова override.

## 🤔 Що було складно
<!-- Чесно запиши що було незрозуміло — це допоможе відстежити прогрес -->
-   розібратися з логікою ініціалізаторів при успадкуванні (Checkpoint 7). зрозумів важливе правило: дочірній клас має спочатку ініціалізувати свої власні унікальні властивості (self.isTame = isTame), а вже потім просити батьківський клас ініціалізувати його базові властивості через super.init().

## 💻 Код дня
<!-- Вставити найцікавіший фрагмент коду або посилання на файл -->
```swift
class Familiar {
    var name: String
    init(name: String) {
        self.name = name
    }
    
    func castSpell() {
        print("✨ \(name) чаклує базове заклинання!")
    }
}

class DragonFamiliar: Familiar {
    override func castSpell() {
        print("🔥 \(name) дихає вогнем!")
    }
}

let spirit = Familiar(name: "Каспер")
let spyro = DragonFamiliar(name: "Спайро")
spirit.castSpell()
spyro.castSpell()
```

## 📝 Нотатки з обговорення з AI
<!-- Короткий підсумок обговорення -->
-   розібрали, чому Swift іноді видає помилку "Return from initializer without initializing all stored properties". також закріпили синтаксис виклику методів на екземплярах: комп'ютеру завжди треба чітко вказувати, хто саме виконує дію (наприклад, spirit.castSpell(), а не просто castSpell()). написали класну RPG-задачку з фамільярами для закріплення успадкування.

## 🖼️ Скріншоти (якщо є)
<!-- Додай скріншоти UI якщо працював з SwiftUI -->
<!-- ![Опис](screenshot.png) -->

## ✅ Прогрес: [12]/101
